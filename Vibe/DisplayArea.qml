import QtQuick
import QtQuick.Controls

// 视频显示区域
Rectangle {

    color: "#645757"
    border.color: "#ffffff"
    border.width: 1

    // 意义不明可以删除
    Label {
        anchors.centerIn: parent
        text: "视频播放区域"
        color: "#FFFFFF"
    }
}